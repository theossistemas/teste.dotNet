@model Sistema.Models.Livros

@{
    ViewData["Title"] = "Detalhes";
}

<div>
    <h2>Informações Livros</h2>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Descricao)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.valor)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.valor)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.id"><button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-pencil"></span> Editar registro</button></a> 
    <a asp-action="Index"><button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-share-alt"></span> Retornar a lista</button></a>
</div>
